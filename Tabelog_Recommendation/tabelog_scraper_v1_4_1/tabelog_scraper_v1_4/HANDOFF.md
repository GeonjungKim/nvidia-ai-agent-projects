# 이어가기 메모 (다음 채팅용 핸드오프)

이 메모 + `tabelog_scraper/` 폴더(v1.4) + `tabelog-recommender-requirements-v1.2.md`를
**프로젝트 지식(Project Knowledge)에 첨부**하면 다음 채팅에서 끊김 없이 이어집니다.

## 지금까지 (2026-07-02 ~ 07-03)

타베로그 리뷰어 방문식당 → 지역×장르 추천 앱. 수업 내 비공개 프로젝트.
Phase 0(수집기 크롬 익스텐션) 구현·검증 완료 → v1.4 파일럿 도구 탑재 → **v1.4.2: 파일럿 단계 A(도쿄 감사) 완료**.

### 파일럿 단계 A 결과 (2026-07-04, tabelog_audit_maro_LstPrf_1783131396583.json)
- **셀렉터 일반화 확정(도쿄)**: 30/30 자식 전부 HTTP 200·mode=card·카드 20·오류 0·리다이렉트 0.
  종합점/방문월/리뷰URL null 0. 뱃지 598개, 변형 클래스 82종 — 전부 3종 접두사로 판독(unknown 0).
- **개인점 null 18건 발견**(A1327×5, A1330×13) — 무평점 방문 엔트리 실재, nullable 스키마 정상 동작.
- **건수 3중 불일치(미해결)**: 라이브 DOM(베어) 9399 / 정규화 fetch 9465 / Σ자식(정규화) 9431.
  ⑤(07-02)엔 베어=장식풀세트=9399 일치 → +66이 파라미터 효과인지 캐시/스테일인지 판별 실험 대기.
  동일조건(정규화) Δ = 9465−9431 = **+34 미커버** — 가설: 伊豆諸島・小笠原(패널 nolink, 링크 수확
  불가) 또는 지역 미분류(폐점 등). 판별 실험 대기.
- 1,200 초과 자식 없음(최대 銀座 624) → OI-6은 이 계층에서 판정 불가, PG=60/61 프로브로 대체 판정.

### 확정 사실 (실측)
- 지역축(서로소): `pal`(도도부현) → `LstPrf`(2차 A코드) → `LstAre`(3차)
- 장르축(중복): `Cat`(대) → `LstCat`(중) → `LstCatD`(소), 대체표기 `genre_name`
- pal=tokyo의 자식 = LstPrf **A1301~A1330 정확히 30개**(중복 0), 장르 1단계 = Cat **6종(RC/MC/SC/BC/YC/ZZ)**
- 지역·장르 동시 적용 가능. 페이지당 20건. `PG`=페이지. 총건수 "全 N 件"(maro×tokyo=9399, 장식 파라미터 무관 — OI-8 재확정).
- **수상 뱃지 마크업 확정(v1.4 신규, 이전 세션 '없음' 주장은 오류였음)**: 카드 내
  `div.simple-rvw__award-badge` > `span.c-badge-{award|hyakumeiten|hot-restaurant}--{YYYY}{수식}`
  3종, 실측 페이지에서 36개/20카드 전수 판독 성공. 상세는 CHANGELOG_v1_4.md §1.
- robots.txt: `*`엔 Crawl-delay 없음(5초는 검색봇 값 → 자율 하한 채택). `/rvwr/*/visitdtl/` Disallow.
- 리뷰 상세 경로는 `/rvwr/{slug}/rvwdtl/B{id}/` (visitdtl과 다름).

### 코드 상태 (v1.4)
- `parsers.js`(파싱·URL, chrome 비의존) + `content.js`(상태기계) 분리 유지.
- **parseAwards**: 뱃지 구조 파서. awards 스키마 `string[]` → **`object[]`** ({type, year, rank|category, label}).
- **커버리지 감사**(content.js, 읽기 전용): 자식 한 단계를 fetch+DOMParser로 검산, JSON 리포트 자동 다운로드.
- **스코프 크롤**(popup): 시작 노드 URL 입력 시 그 서브트리만 순회(격리는 엄격 자식 규칙의 따름정리, 합성 테스트로 검증).
- 버그 수정: 백오프 한계 시 reviewerQueue 건너뜀(popNext로 통합).
- 테스트 **67/67** 통과(logic 34 + dom 33, 실측 픽스처에 배포 코드 직접 실행). v1.3은 동일 픽스처에서 24/25(유일 실패=awards)였음.
- v1.4.1: 감사 fetch 가드(SP 리다이렉트/CORS 실측 대응). v1.4.2: 부모 3중 측정 + nolink 기록.

### 해결된 OI: 1, 2, 2a, 7, 8, 11 / FR-01b(구 OI-10) / 뱃지 마크업(구 파일럿 목표 3)
### 남은 OI
- OI-3: rvwdtl 경로 접근의 규약 적합성 최종 확인(C-8) — 리뷰 본문 수집(FR-08) 착수 전 필수
- OI-4: 리뷰어 선정 기준(Phase 4)
- OI-5: 속성 라벨 스키마 초안(Phase 2 선행물)
- OI-6: 임계 1,200(60페이지) 실측 재확인 — 도쿄 LstPrf엔 1,200 초과 노드 없음 → **PG=60/61 콘솔 프로브로 판정**(스니펫 배포됨)
- OI-8(재개방): 장식 파라미터의 건수 영향 — 07-02엔 효과 0, 07-04엔 +66 관측(원인 미확정: 파라미터 vs 캐시/스테일). 판별 스니펫 결과 대기
- OI-9: 地図表示 좌표(FR-12, 낮음)
- OI-12(신설): 커버리지 +34 미커버의 정체 — 伊豆諸島・小笠原(nolink) vs 지역 미분류. 판별: 공개 검색에서 伊豆 A코드 실측 후 `?pal=tokyo&LstPrf=코드` 건수 확인, 또는 Phase 1 전량 수집 후 area2 분포 대조

## 다음 할 일 (우선순위)
1. **판별 스니펫**(콘솔, 8요청·~1분): 베어/파라미터별/⑤재현/PG=60·61 건수 → OI-8·OI-6 판정
2. **파일럿 단계 B**: 스코프 크롤 — ① A1330 福生・青梅周辺(76건, 스모크) → export
   ② A1301 銀座(624건, 볼륨) → export → 각 JSON 검산(records vs count, losses, awards 실데이터)
3. (선택) 단계 C: pal=osaka 감사로 비도쿄 일반화 확인
4. OI-12 판별(伊豆 코드 실측 1요청)
2. 파일럿 OK → Phase 1(DB 적재 FR-09 + 필터·랭킹 FR-11) 착수 — awards 신스키마 반영
3. Phase 2 준비: 속성 라벨 스키마(OI-5) 초안

## 작업 원칙 (사용자 명시)
추측 금지. 신뢰도 높은 자료·사실·논문 근거. 코드는 실측 데이터로 검증 후 제시.
한국어 반말. 확정 사실 vs 가설 명확히 구분.
